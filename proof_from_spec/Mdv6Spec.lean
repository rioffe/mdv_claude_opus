/-
Mdv6Spec — the root module.

Imports the three concern modules of the mdv6 specification model, in dependency
order: the normative constants (`Spec`), the transcription of the spec's tables
(`Model`), and the proofs (`Theorems`).
-/
import Mdv6Spec.Mdv6.Spec
import Mdv6Spec.Mdv6.Model
import Mdv6Spec.Mdv6.Theorems