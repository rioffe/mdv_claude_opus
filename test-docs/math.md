# Math

Inline $E = mc^2$ and display below (T-07, R-12).

$$
\int_0^\infty e^{-x^2}\,dx = \frac{\sqrt{\pi}}{2}
$$

$$r = \frac{\text{annual rate}}{\text{payments per year}}$$

## Heading with $\Sigma$ in it

## $\pi$ at h2 size, $\frac{a}{b}$ too

## _Draft_ notes

## snake_case_name

### Environments

$$
f(x) = \begin{cases} 1 & x > 0 \\ 0 & \text{else} \end{cases}
\qquad
\begin{pmatrix} 1 & 2 \\ 3 & 4 \end{pmatrix}
\qquad
\begin{aligned} a &= b \\ c &= d \end{aligned}
$$

### In lists, quotes and tables

- item with $x^2 + y^2 = z^2$
- $$E = mc^2$$

> quoted $\alpha \leq \beta$ text

| expression | value |
| ---------- | ----- |
| $\sqrt{2}$ | 1.414 |
| $$\frac{1}{3}$$ | 0.333 |

### Boxed and registered symbols

$\boxed{x = 1}$ and $a \gtrsim b \leqslant c \implies d \hookrightarrow e$, $\varnothing \checkmark \iint \intercal$.

### Must NOT become math

Prices: $5 and $10, a range $5-$10, and $100/month. Escaped \$x\$ stays literal. An empty $$ pair. Code `$HOME` is code.

### Errors

Unknown command: $\unknowncmd{x}$

$$
\frac{a
$$

Paragraph after the errors.
