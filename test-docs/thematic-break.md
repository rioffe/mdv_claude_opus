# Thematic breaks and smart typography

"Quoted" text with -- dashes --- and an ellipsis... The flag `--verbose` stays straight, and so does --flag inline.

---

* * *

___

Text after the rules with 'single quotes'.
