import XCTest
@testable import mdv6Core

final class RenderPipelineTests: XCTestCase {
    /// W0 placeholder — replaced by the wave that owns this group (R-37).
    func testTargetBuilds() { XCTAssertTrue(true) }
}
